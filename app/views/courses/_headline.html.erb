<section class='headline-unit'>
  <div class='container'>
    <h1><%= @course.tagline %></h1>
    <h4>EdWel's <%= @course.title %> in <%= @location.name %></h4>
    <span class="glyphicon glyphicon-circle-arrow-down navigator"></span>
  </div>
</section>