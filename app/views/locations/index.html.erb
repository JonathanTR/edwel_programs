<section id="headline">
  <div class="container">
    <h1>Welcome to EdWel</h1>
    <h3>These are our locations:</h3>
    <% @locations.each do |location| %>
    <p><%= link_to location.name, location_path(location) %></p>
    <% end %>
  </div>
</section>